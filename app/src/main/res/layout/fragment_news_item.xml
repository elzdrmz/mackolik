<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:background="@color/black">

        <ImageView
            android:id="@+id/fragment_news_item_image"
            android:layout_width="100dp"
            android:layout_height="match_parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            />

        <TextView
            android:id="@+id/fragment_news_item_title"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:textColor="@color/white"
            android:textSize="16sp"
            app:layout_constraintStart_toEndOf="@id/fragment_news_item_image"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            android:layout_marginEnd="16dp"
            android:layout_marginStart="16dp"
            android:layout_marginTop="24dp"
            android:maxLines="2"
            tools:text="This is a title" />

        <TextView
            android:id="@+id/fragment_news_item_date"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="#F00"
            android:textSize="12sp"
            app:layout_constraintTop_toBottomOf="@id/fragment_news_item_title"
            app:layout_constraintStart_toEndOf="@id/fragment_news_item_image"
            android:layout_marginTop="8dp"
            android:layout_marginStart="16dp"
            tools:text="Wednesday, 7 December 2011"
            />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>